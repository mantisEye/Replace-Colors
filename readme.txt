Description:
This class takes 2 pallets and an image. First pallet(input_pallet1.png) represents the colors of
the original image while the 2nd pallet(input_pallet2.png) represents what to change the colors to.

Original image: input_toChange.png
New image to create: output_newImage.png

Pallets don't have to be same size. 